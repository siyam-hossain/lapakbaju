spring:
  application:
    name: lapakbaju-store

  datasource:
    url: jdbc:mysql://localhost:3306/lapakbaju?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: ""
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    # Spring Boot automatically detects MySQLDialect from the datasource URL,
    # so keeping 'properties.hibernate.dialect' is optional.